# Zava System - Python Dependencies

# Web Framework
Flask>=3.0.0
gunicorn>=21.2.0
Werkzeug>=3.0.0

# Azure AI and Cognitive Services
azure-ai-projects>=1.0.0
azure-identity>=1.12.0
azure-core>=1.26.0
openai>=1.30.0  # Azure OpenAI Assistants API
azure-ai-vision-imageanalysis>=1.0.0  # Azure AI Vision for OCR and image analysis
azure-cognitiveservices-speech>=1.35.0  # Azure Speech Services for voice input/output

# Azure Cosmos DB
azure-cosmos>=4.5.0

# Microsoft Agent Framework v1.8.1 (GA)
# agent-framework-azure-ai removed — superseded by agent-framework-foundry
agent-framework-core>=1.8.1
agent-framework-openai>=1.8.1        # OpenAIChatClient (renamed from OpenAIChatCompletionClient)
agent-framework-foundry>=1.8.1      # FoundryChatClient, FoundryMemoryProvider (replaces agent-framework-azure-ai)
agent-framework-orchestrations      # SequentialBuilder, HandoffBuilder, GroupChatBuilder, MagenticBuilder
# HTTP server hosting for Agent Inspector / agentdev CLI
azure-ai-agentserver-agentframework==1.0.0b17
azure-ai-agentserver-core==1.0.0b16  # held at b16 — b5 is a major-version break, review separately
# Debug tooling
debugpy>=1.8.0
agent-dev-cli

# Data Processing
python-dotenv>=1.0.0
pydantic>=2.0.0

# Utilities
requests>=2.31.0
python-dateutil>=2.8.2
aiohttp>=3.9.0  # For async HTTP requests (Security Copilot integration)
faker>=20.0.0  # Generate realistic test data

# Email and OCR Support for Fraud Detection
pytesseract>=0.3.10  # OCR text extraction from screenshots
Pillow>=10.0.0  # Image processing
extract-msg>=0.45.0  # Outlook MSG file parsing

# Route Optimization
# Note: Azure Maps requires subscription key in environment variable AZURE_MAPS_SUBSCRIPTION_KEY
# Get from: https://portal.azure.com -> Create Azure Maps Account -> Authentication -> Primary Key
# Migrated from Bing Maps (deprecated June 2025) to Azure Maps
Pillow>=10.0.0       # Image processing for OCR
# Note: pytesseract requires Tesseract-OCR to be installed on the system
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Mac: brew install tesseract
# Linux: apt-get install tesseract-ocr

# Optional: Advanced .MSG file parsing
# extract-msg>=0.45.0  # Uncomment for better Outlook .MSG file parsing

# Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
